services:
  bot:
    build: .
    ports:
      - "5173:5173"
      - "3001:3001"
    volumes:
      - ./data:/app/data
      - ./.env:/app/.env
    restart: unless-stopped
    environment:
      - NODE_ENV=production
